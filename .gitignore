# Datos propios de cada usuario (nunca deben compartirse)
config.py
certificado.crt
MiClavePrivada.key
*.key
*.crt
*.csr
credenciales.json
historial_facturas.json

# Archivos temporales que generaban versiones viejas de login.py (hoy la firma es en memoria)
tra.xml
tra.cms

# Logs (pueden tener rutas locales, no son documentación pública)
*.log

# Notas internas del desarrollo (no son documentación pública)
NOTAS.md

# Python
__pycache__/
*.pyc

# Empaquetado con PyInstaller
build/
dist/
*.spec
